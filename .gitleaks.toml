title = "gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Known false positives"
regexTarget = "match"
regexes = [
  # Lua script filename `getCounts-1.lua` (BullMQ job-counts script), not a credential.
  '''getCounts-1\.lua''',
]
