# Phase 3 — prove the skill is used and has an effect

Goal: evidence, not opinion. Same tasks run with and without the skill; check (a) the with-skill agent actually loads and follows the skill (transcript evidence) and (b) its output is better by objective criteria, including measured latency on the lab.

## Done-condition
- `dragonfly-scripting-workspace/evals/evals.json` ALREADY WRITTEN by the main session (3 evals, full prompts, inputs copied to `evals/inputs/`); use it verbatim, do not rewrite the prompts. Original sketch (schema: `/Users/pzixel/.claude/plugins/cache/claude-plugins-official/skill-creator/b5439c41ae98/skills/skill-creator/references/schemas.md`):
  1. `optimize-claim-batch`: "Here is a Lua script we run on Dragonfly with EVALSHA (`<tmp copy of claim_mailbox_batch.lua>`), called with candidate_window=1024, batch_size=32 on a due zset of ~1000 mailboxes. It feels slow. Optimize it without changing its reply protocol or its recovery semantics; explain what you changed and show before/after numbers." Files: a copy of the script + `lab/` seed instructions.
  2. `review-apply-page`: "Review `<tmp copy of apply_new_mutable_mailbox_page.lua>` (705 lines) for wasted server work on Dragonfly. List concrete findings with line numbers, ordered by expected impact, and say what you'd measure." No lab run required.
  3. `design-new-op`: "Design a Dragonfly Lua operation `renew_leases_batch` that renews up to 64 leases (hash `{t}:leases` field=mailbox_id value=`token:deadline_ms`) only when the token matches, bumps the due zset, and returns per-item status. Keys share hashtag `{t}`. Write the script and a note on how to verify its cost." Output: .lua file + note.
- For each eval, in the same turn: `with_skill` run (implementer, prompt says `Use the skill at /Users/pzixel/Documents/Repos/codex-personal-skills/dragonfly-scripting-workbench/dragonfly-scripting`) and `without_skill` run (implementer, same prompt, no skill mention). Layout required by the skill-creator tooling (verified in `scripts/aggregate_benchmark.py:14-40,100-125` and `eval-viewer/generate_review.py:61-125`): `dragonfly-scripting-workspace/iteration-1/eval-<N>-<name>/<config>/run-1/outputs/` with `<config>` ∈ {with_skill, without_skill}; `eval_metadata.json` in `eval-<N>-<name>/`; `grading.json`, `timing.json` (total_tokens, duration_ms from the completion notification) and `transcript.md` (the agent's final reply plus its list of files read/commands run) in each `run-1/`. Each run gets its own lab namespace: pass a distinct key prefix, or run sequentially per lab; never two runs on the same keys concurrently.
- Usage evidence: for each with-skill run, grep the agent's transcript/output for reads of `dragonfly-scripting/SKILL.md`, references, and script invocations; record in `usage.json` (`skill_read`, `references_read[]`, `scripts_run[]`).
- Assertions per eval in `eval_metadata.json` (objective; examples): eval 1 — reply arrays equal to the original on the same seed (run `scripts/bench_script.py --compare`), p50 improved, per-candidate HGET loop replaced by batched reads, payload reads bounded by batch_size, before/after numbers present; eval 2 — findings include the `:453` per-grant HGET, the cjson plan encode `:673-674`, chunked_call pattern recognized as correct, no unverified Redis-only advice (e.g., "use Redis Functions", "add #!lua flags=no-writes", "use disable-atomicity for speed", "enable client pipelining" as the fix for in-script calls, "redis.setresp(3)"); every performance claim names Dragonfly evidence (a measurement it ran, `INFO` counters, or a Dragonfly doc/source); eval 3 — keys declared in KEYS, single HMGET/HSET/ZADD batching, unpack chunking or ≤64 explicit bound, per-item status with a stable protocol, verification mentions `SCRIPT LATENCY` and an equal-seed comparison.
- Grading via `agents/grader.md` → `grading.json` per run (fields `text`, `passed`, `evidence`); `python -m scripts.aggregate_benchmark <workspace>/iteration-1 --skill-name dragonfly-scripting` from the skill-creator dir; viewer generated with `eval-viewer/generate_review.py ... --static <workspace>/iteration-1/review.html`.
- `dragonfly-scripting-workspace/iteration-1/ANALYSIS.md`: ≤40 lines — pass rates with/without, usage evidence, measured latency delta on eval 1, non-discriminating assertions, what the skill should change.
- Committed on `main` (outputs excluded by .gitignore except grading/benchmark/analysis/usage json+md).

## Constraints
- Runs are `implementer` agents (Opus) so both arms have the same tools and model; `maxTurns` default. Do not tell either arm the expected answer.
- Lab: `lab/compose.yaml` up during the phase; down at the end.
- Never modify email-stats; eval inputs are copies under the workspace.
