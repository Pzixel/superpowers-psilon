# Review 2 — `dragonfly-scripting` skill text, commit `800ad8e` (main)

Scope: `dragonfly-scripting/SKILL.md` + `references/*.md` against phase-2 done-condition clause 5 (criteria 1-6) and the decided items (phase-2.md:19-27). Scripts reviewed only for interface fidelity of the text.

Verdict: **REJECT** — four criterion-1 violations, each a one-line fix. Everything else is non-blocking.

## Blocker index

| # | finding | severity | file:line | impact | minimum fix |
|---|---|---|---|---|---|
| B1 | Worked-example numbers (orig ~42.8 ms, hmget ~8.7 ms, 164 fields, 33 clock positions, 101.9 vs 9.4 ms) exist nowhere in `lab/RESULTS.md` or `lab/results/*` | blocking (crit 1) | `SKILL.md:137-139`, `references/measurements.md:67-70` | reader quotes untraceable absolutes; "noisier machine" run has no flags/threads/N recorded | delete the six numbers (keep "direction only" + Q7 citation) or record the run in RESULTS.md with conditions and cite it |
| B2 | "499 µs idle vs 149 µs cached" pairs the idle fresh-text cell with the **4-loaders** cached cell; idle cached is 162 µs | blocking (crit 1, conditions) | `SKILL.md:79-80`, `references/lua-patterns.md:109-110` | mixed-condition comparison, the trap the skill itself warns about | "499 vs 162 µs idle, 478 vs 149 µs under 4 loaders (default flags)" |
| B3 | Q3 p50 quoted as 31972; RESULTS.md Q3 table/summary say 31971(.2) | blocking (crit 1) | `SKILL.md:65`, `references/measurements.md:22` | number not in RESULTS.md | write 31971 |
| B4 | "1.5-2.2x different absolutes" — RESULTS Q10 reconciliation gives Q2 1.49x, Q3 2.19x, Q7 1.33x | blocking (crit 1, derived number wrong) | `SKILL.md:120` | understates the spread; not a RESULTS number | "1.3-2.2x" |

Criteria with no blocking findings: 2 (all performance rules carry `[lab Qn]`/`[df-src]`/`[df-doc]` + `[Sn]`; S1-S30 all resolve; no Redis-only performance rule outside the audit table), 3, 5 (detection is section 0, lines 12-28, first thing after the intro), 6 (all eleven required rows present with verdict + evidence: audit.md:9-20).

## Detail — non-blocking (should-fix)

- **N1 (crit 5, detection procedure)** `SKILL.md:23`: "`dragonfly --helpfull | grep lock_on_hashtags` for the server's actual flags" is wrong — `--helpfull` starts a new binary and prints its flag catalogue/defaults, not the running server's configuration (Q9 used it only as an inventory, RESULTS.md Q9). `research/dragonfly-sources.md:231` says only three flags are `CONFIG SET`-mutable; `CONFIG GET lock_on_hashtags` support is unstated anywhere. Failure: reader runs `--helpfull` on the client host, sees `false`, concludes regime (a) while the server is (b). Fix: "the counter diff is authoritative; confirm the flag from the server process's command line (`CONFIG GET lock_on_hashtags` if the build answers it); `--helpfull` only lists the flag surface".
- **N2 (decided item 1, claim_mailbox_batch)** `references/lua-patterns.md:53-56`: item (1) is labelled "pure batching, contract-neutral … 41183 -> 7814 µs with identical replies", but Q7's 5.3x was measured on `claim_mailbox_batch.hmget.lua`, which *also* narrows `record_missing` and defers payloads (RESULTS.md Q7 "Variant sources"; variant header line 8). The isolated value of contract-neutral batching was never measured; "identical replies" holds on the seeded input only. `SKILL.md:140-141,148-152` get this right; the reference contradicts it. Fix: "Q7 measures (1)+(2) together; Q3 bounds (2) at 1.1-1.8x, so most of the 5.3x is (1)".
- **N3 (crit 2, generic / Redis-only behavior)** `references/lua-patterns.md:84-101,105-108`, `SKILL.md:98-100`: array-truncates-at-nil, `false` not `nil`, `call` vs `pcall`, globals are an error, integer status codes, cache-SHA/NOSCRIPT-reload-once, EVAL-not-EVALSHA in pipelines rest only on `[S21][S25-S28]` (Redis docs / client libraries). Not performance rules, so not blocking, but (a) a capable model knows all of it and (b) the Dragonfly-side behaviors (nil truncation, globals rejection, RESP2 null as `false`) are asserted for Dragonfly without a probe or `[df-src]`. Fix: keep only Dragonfly deltas (already in `redis-differences.md:32-34`), or add a Q9-style probe row and tag it; drop §5 lines 105-108.
- **N4 (crit 4, progressive disclosure)** Duplicated verbatim between entrypoint and references: worked example `SKILL.md:133-141` = `measurements.md:64-70`; audit corpus counts `SKILL.md:83-85` = `measurements.md:87-91`; `--lock_on_hashtags` trade-off `SKILL.md:105-111` = `server-flags.md:37-55`; Q5 regime numbers `SKILL.md:17-28` = `execution-model.md:50-59` = `redis-differences.md:40`. Skill-creator :27-28 / :117-121: do not duplicate reference content in the entrypoint. Fix: one sentence + link in SKILL.md for the worked example and the audit counts; keep the regime table and the decision rule in SKILL.md (they change decisions) and cut them from the references, or vice versa.
- **N5 (crit 3/4, description)** `SKILL.md:3`: triggers on every listed intent and excludes generic caching — passes. But it is ~110 words and carries a non-routing sentence ("Every rule is measured on Dragonfly v1.34.0 …"); skill-creator :22 warns against exhaustive lists. Fix: cut that sentence; keep intents + exclusion.
- **N6 (crit 6, verdict vocabulary)** `references/redis-advice-audit.md:9,10,15,16`: verdict cells use free-form phrases ("depends on the regime", "sometimes on default flags, never under …", "harms in regime (b); pointless in regime (a)", "neutral idle, harms under load") instead of leading with one of helps/neutral/harms/absent/unverified. Rows and evidence are complete, so non-blocking. Fix: lead each cell with the fixed word, qualifier after a dash.
- **N7 (crit 1, provenance)** `SKILL.md:83-85`, `measurements.md:89-91`: "66 finding lines (37/24/4/1/0)" is traceable only to `fable-reviews/planned/dragonfly-skill/handoff-2.md:184,227`, not RESULTS.md. It is a tool self-test count, reproducible by running the script, so not treated as a lab number; cite the handoff or drop the split.
- **N8 (tag accuracy)** `references/redis-differences.md:32`: "Lua **5.4.4**", "no `os`, `io`, `package`; `debug` is `nil`" are tagged `[lab Q9][S1][S24]`, but Q9 probed only `_VERSION = Lua 5.4` (results/q9.txt:3933) and S24 is the Lua manual. The 5.4.4 figure comes from `docs/differences.md` (S17, research/dragonfly-sources.md:49) and the library list from `interpreter.cc:352-361` (S4, research/dragonfly-sources.md:268). Fix: retag `[df-doc][S17][df-src interpreter.cc][S4]`.

## Notes (no action required)

- Decided item 2 (`--lock_on_hashtags` as measured trade-off, decision rule, Q11d 81 -> 7712 µs, 1.54 not 1.03): stated as decided at `SKILL.md:105-111` and `server-flags.md:37-55`. No relitigation found.
- Decided item 1: `SKILL.md:140-141,148-152` states the hmget variant is a measurement vehicle, recommends batching every read, and isolates the bound-payload decision at 1.1-1.8x (Q3). Only the `lua-patterns.md` wording (N2) drifts.
- Q10-vs-standalone rule: every ratio in SKILL.md and the references is attributed to Q10 and every absolute to the standalone run (checked: SKILL.md:54-66, audit.md:11, lua-patterns.md:32-37,57-59, server-flags.md:40-42, execution-model.md:73-76). 1-key vs 8-key numbers always carry their shape (checked SKILL.md:17,63,78,106; audit.md:13,15; execution-model.md:50-59).
- Spot-checked against RESULTS.md and matching: 1.01/26.95/27.00/25.26/23.17/1.54/30.3/30.1/28.05->2.93/27.40 (Q5); 5496/4698/0.7/5.1 (Q1); 5846/539/9295/1920 (Q2); 34136/1.1x (Q3); 1905/15070/33766783 (Q4); 4679/4484/+4.3%/569/402/+41.6% (Q6); 41183/7814/52222/17150/663192/49792 (Q7); 33219/33084/169/191 (Q8); 8163/8164 (Q9); 9.9/2.0/1.3/1.8/7.6/2.6, 339/398/108/110 (Q10); 1860/480/446/186/2379/1419/175/397/1026/981/460/472, 83.7/109.6/146-149, 131/150/99/118/0.9x/0.8x, 499/478/162/149/7787/7712/81/115-125/8894B (Q11).
- Script interfaces match the text: `lua_call_audit.py` RULES (5 ids, :14-20), `--rules/--rule/--exclude/--count` (:284-289), exit 0 (:4); `script_latency.py --sha` prefix, `--watch [SECONDS]` (:56-57); `bench_script.py` spec keys and `--compare`/`--reseed`/exit 2 (:7-31,:145-149); `lab.sh up|down|status [--single]`, `down -v` (:11-17,:46).
- No web pages were read; `SOURCES.md` untouched.
