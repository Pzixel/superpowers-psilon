interface:
  display_name: "Superpowers: Parallel Agents (psilon)"
  short_description: "Delegate independent substantial workstreams"
  default_prompt: "Use $superpowers-dispatching-parallel-agents-psilon to scope and dispatch these independent substantial workstreams safely."

policy:
  allow_implicit_invocation: true
