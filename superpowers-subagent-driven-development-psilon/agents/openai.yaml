interface:
  display_name: "Superpowers: Subagent Development (psilon)"
  short_description: "Delegate large decomposed implementations"
  default_prompt: "Use $superpowers-subagent-driven-development-psilon to execute the ready tasks in this large decomposed plan with bounded subagent ownership."

policy:
  allow_implicit_invocation: true
